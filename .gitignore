# Secrets — SimpleFIN Access URL is a banking credential. Never commit.
data/secrets/
*.access-url

# Raw fetch archive and learned state
data/raw/
data/eval/

# Backfill resume state: per-run progress, machine-written after every
# window (bookkeeper/ingest/backfill.py). Operational, not a ledger artifact.
data/backfill/

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Node
node_modules/
.next/
.env
.env.local
*.tsbuildinfo

# SQLite
*.db
*.db-journal
*.sqlite
*.sqlite3

# OMC operational state
.omc/

# SQLite sidecar files (web/data/local.db-shm, -wal)
*.db-shm
*.db-wal
